<apex:page controller="ChatterMsgBoardController" action="{!initAdminGroup}" tabStyle="AdminMessageBoard__tab" lightningStylesheets="true">
    <apex:sectionHeader title="{!grp.Name}" />
    <apex:pageBlock >
        <chatter:feed entityId="{!grp.Id}" />
    </apex:pageBlock>
</apex:page>