<apex:page controller="ResultsController" tabStyle="Results__tab" >
    <apex:includeScript value="https://www.google.com/jsapi" />
    <apex:includeScript value="{!$Resource.jquery}" />
    <script type="text/javascript">
    var selectedWeek = '{!selectedWeek}';
    
    // Load the Visualization API and the core chart package.
    google.load('visualization', '1.0', {'packages':['corechart']});
        
    // Set a callback to run when the Google Visualization API is loaded.
    google.setOnLoadCallback(refreshResults);

    // Callback that creates and populates a data table,
    // instantiates the pie chart, passes in the data and
    // draws it.
    function refreshResults() {
        Visualforce.remoting.Manager.invokeAction(
            '{!$RemoteAction.ResultsController.getWeeklyResults}', selectedWeek,
            function(results, event) {
                if (event.status) {
                    if (results.pointsAvailable && results.rWrappers && results.rWrappers.length > 0) {
                        $('#wrNotAvailable').hide();
                        drawPointsChart(results, 'Points Scored - Week ' + selectedWeek, 'currentPointsDiv');
                        drawPercentageChart(results, 'Win Percentage - Week ' + selectedWeek, 'currentPercentageDiv');
                        $('#wrRow').show();
                    } else {
                        $('#wrNotAvailable').show();
                        $('#wrRow').hide();
                    }
                } else {
                    flashErrorMessage(event.message);
                }
            },
            {escape: false}
        );
        Visualforce.remoting.Manager.invokeAction(
            '{!$RemoteAction.ResultsController.getSeasonalResults}',
            function(results, event) {
                if (event.status) {
                    if (results.pointsAvailable && results.rWrappers && results.rWrappers.length > 0) {
                        $('#srNotAvailable').hide();
                        drawPointsChart(results, 'Points Scored - Season', 'seasonPointsDiv');
                        drawPercentageChart(results, 'Win Percentage - Season', 'seasonPercentageDiv');
                        $('#srRow').show();
                    } else {
                        $('#srNotAvailable').show();
                        $('#srRow').hide();
                    }
                } else {
                    flashErrorMessage(event.message);
                }
            },
            {escape: false}
        );
        setLastRefreshed();
        setTimeout('refreshResults()', 120000);
    }
    
    function drawPointsChart(results, chartTitle, domId) {
        var pointsData = new google.visualization.DataTable();
        pointsData.addColumn({type : 'string', label : 'Nickname'});
        pointsData.addColumn({type : 'number', label : 'Points Scored'});
        for (var i=0; i < results.rWrappers.length; i++) {
            pointsData.addRow([{v:results.rWrappers[i].nickname}, {v:results.rWrappers[i].pointsScored, f:results.rWrappers[i].pointsScored.toFixed(1)}]);
        }
        var options = {
            title : chartTitle,
            colors : ['blue'],
            height: Math.max(results.rWrappers.length * 25, 500),
            width: 500,
            chartArea : {
                left: 150
            },
            hAxis : { 
                format : '#0.0',
                title : 'Total points available: ' + results.pointsAvailable.toFixed(1)
            },
            legend : { 
                position : 'none' 
            }
        }
        var chart = new google.visualization.BarChart(document.getElementById(domId));
        chart.draw(pointsData, options);
    }
    
    function drawPercentageChart(results, chartTitle, domId) {
        var percentageData = new google.visualization.DataTable();
        percentageData.addColumn({type : 'string', label : 'Nickname'});
        percentageData.addColumn({type : 'number', label : 'Win Percentage'});
        for (var i=0; i < results.rWrappers.length; i++) {
            percentageData.addRow([{v:results.rWrappers[i].nickname}, {v:results.rWrappers[i].winPercentage, f:results.rWrappers[i].winPercentage.toFixed(3)}]);
        }
        var options = {
            title : chartTitle,
            colors : ['red'],
            height: Math.max(results.rWrappers.length * 25, 500),
            width: 500,
            chartArea : {
                left: 150
            },
            hAxis : { 
                format : '0.000'
            },
            legend : { 
                position : 'none' 
            }
        }
        var chart = new google.visualization.BarChart(document.getElementById(domId));
        chart.draw(percentageData, options);
    }
    
    function setLastRefreshed() {
        var currentDate = new Date();
        var day = currentDate.getDate();
        var month = currentDate.getMonth() + 1;
        var year = currentDate.getFullYear();
        var hours = currentDate.getHours();
        var minutes = currentDate.getMinutes();
        if (minutes < 10) {
            minutes = '0' + minutes;
        }
        var suffix = 'AM';
        if (hours >= 12) {
            suffix = 'PM';
            hours = hours - 12;
        }
        if (hours == 0) {
            hours = 12;
        }
        $('#lastRefreshed').html('Last refreshed: ' + month + '/' + day + '/' + year + ' ' + hours + ':' + minutes + ' ' + suffix);
    }
    
    function flashErrorMessage(msg) {
        $('#errorMsg').html(msg);
        $('#errorMsg').fadeIn(600, function() {
            setTimeout('$(\'#errorMsg\').fadeOut(600)', 10000);
        });
    }

    $(document).ready(function() {
        $('#errorMsg').hide();
        $('#wrNotAvailable').hide();
        $('#wrRow').hide();
        $('#srNotAvailable').hide();
        $('#srRow').hide();
    });
    </script>
    
    <h1 id="errorMsg" style="font-size:1.2em; font-weight:bold; color:red;"></h1>
    <p id="lastRefreshed" style="font-size:1.2em; font-weight:bold" ></p>
    <p style="font-size:1.1em;" >
        This page will refresh automatically every two minutes.
        <apex:outputPanel layout="inline" rendered="{!VALUE(currentWeek) > 1}" >
            <apex:outputLabel value="Click here to view results for prior weeks: " />
            <apex:repeat value="{!weeks}" var="weekNumber" >
                &nbsp;
                <apex:outputLink value="{!$Page.Results}" 
                    style="{!IF(weekNumber == selectedWeek, 'font-weight:bold; color:red', 'font-weight:normal;')}" >
                    <apex:outputText value="{!weekNumber}" />
                    <apex:param name="week" value="{!weekNumber}" />
                </apex:outputLink>
            </apex:repeat> 
        </apex:outputPanel>
    </p>
    <table id="resultsTable" >
        <tbody>
            <tr id="wrNotAvailable" >
                <td style="font-size:1.2em; font-weight:bold; color:orange; padding-bottom:20px;" >Weekly results not yet available</td>
            </tr>
            <tr id="wrRow" >
                <td>
                    <div id="currentPointsDiv"></div>
                </td>
                <td>
                    <div id="currentPercentageDiv"></div>
                </td>
            </tr>
            <tr id="srNotAvailable" >
                <td style="font-size:1.2em; font-weight:bold; color:orange;" >Seasonal results not yet available</td>
            </tr>
            <tr id="srRow" >
                <td>
                    <div id="seasonPointsDiv"></div>
                </td>
                <td>
                    <div id="seasonPercentageDiv"></div>
                </td>
            </tr>
        </tbody>
    </table>
</apex:page>